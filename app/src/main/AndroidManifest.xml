<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pc.joe.medapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.AppCompat"
        android:windowSoftInputMode="adjustNothing">
        <activity android:name=".TestActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".ViewAppointment" />
        <activity android:name=".ScheduleAppointments" />
        <activity android:name=".ViewRecords" />
        <activity android:name=".userSetup" />
        <activity
            android:name=".ViewReports"
            android:label="@string/title_activity_view_reports"></activity>
    </application>

</manifest>